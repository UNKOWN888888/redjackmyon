# Fixtures

Put copied DOM snippets here when a live table element needs repeatable analysis.

Recommended filenames:

- `seat-5-betting-open.html`
- `chips-tray-750.html`
- `insurance-panel.html`
- `autoplay-control-running.html`

Keep these snippets small and focused on the failing element. Codex can then write a Node fixture test against the relevant `src/partials/*.js` function instead of relying only on screenshots.
